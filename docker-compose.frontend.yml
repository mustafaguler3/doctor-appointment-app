version: '3.8'

services:
  backend:
    image: mustafaguler4/doctor-app-backend
    ports:
      - "8080:8080"

  frontend:
    image: mustafaguler4/doctor-app-frontend
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend